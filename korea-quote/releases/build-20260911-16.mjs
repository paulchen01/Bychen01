/** v16: SmartStore self-service beta. Customer completes Naver verification in their own browser, then shares the verified tab or uploads a screenshot for local OCR. Existing pricing/B2/APIs remain unchanged. */
import fs from 'node:fs';import path from 'node:path';import assert from 'node:assert/strict';import{createHash}from'node:crypto';
const BASE='https://raw.githubusercontent.com/paulchen01/Bychen01/main/korea-quote/releases/site-20260911-15.json';
const EXPECTED='0c29819a70f9ebfda47ce1eed071a6bbe958c874c3b87165a19fcde21059b3ea';
const VERSION='2026-09-11.16',sha=b=>createHash('sha256').update(b).digest('hex');
const r=await fetch(BASE,{signal:AbortSignal.timeout(40000)});assert.equal(r.status,200);const raw=Buffer.from(await r.arrayBuffer());assert.equal(sha(raw),EXPECTED);const base=JSON.parse(raw),files=new Map();
for(const f of base.files){const b=Buffer.from(f.data,f.encoding==='base64'?'base64':'utf8');assert.equal(sha(b),base.hashes[f.file]);files.set(f.file,b);}
const beta=`(()=>{\n'use strict';\nconst $=s=>document.querySelector(s);\nconst isSmart=u=>{try{const x=new URL(u);return /(^|\\.)smartstore\\.naver\\.com$/i.test(x.hostname)&&/\\/products\\/\\d+/.test(x.pathname)}catch{return false}};\nconst esc=s=>String(s).replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[c]));\nconst style=document.createElement('style');style.textContent=\`.ss-beta{display:none;margin:16px 0;padding:16px;background:#f3fbf3;border:1px solid #b9ddb9;border-left:4px solid #25a244;border-radius:14px;color:#2f5138}.ss-beta.show{display:block}.ss-title{display:flex;gap:8px;align-items:center;flex-wrap:wrap}.ss-title strong{font-size:16px}.ss-badge{font-size:10px;padding:2px 8px;background:#daf1da;border-radius:999px}.ss-help{margin:10px 0;font-size:12px;line-height:1.8}.ss-translate{background:#fff;border:1px solid #d9ebd9;border-radius:10px;padding:10px;margin:10px 0;font-size:11px;line-height:1.75}.ss-actions{display:flex;gap:8px;flex-wrap:wrap}.ss-actions button,.ss-actions a{appearance:none;border:1px solid #5d9c68;background:#fff;color:#2b6338;border-radius:9px;padding:9px 11px;font:inherit;text-decoration:none;cursor:pointer}.ss-actions .primary{background:#2f8f46;color:#fff;border-color:#2f8f46}.ss-status{margin-top:10px;font-size:11px;line-height:1.7}.ss-candidates{display:grid;gap:7px;margin-top:10px}.ss-price{width:100%;text-align:left;background:#fff;border:1px solid #c9dfc9;border-radius:9px;padding:10px;cursor:pointer}.ss-private{font-size:10px;color:#58705e;margin-top:8px}.ss-upload{display:none}.ss-preview{max-width:100%;margin-top:8px;border-radius:8px}.ss-spinner{display:inline-block;width:13px;height:13px;border:2px solid #b9d9bf;border-top-color:#2f8f46;border-radius:50%;animation:ssspin .8s linear infinite;vertical-align:-2px;margin-right:5px}@keyframes ssspin{to{transform:rotate(360deg)}}@media(max-width:480px){.ss-actions>*{flex:1 1 100%;text-align:center}}\`;document.head.appendChild(style);\nconst url=$('#url');if(!url)return;const host=url.closest('form')||url.parentElement;const box=document.createElement('section');box.className='ss-beta';box.id='smartstoreBeta';box.innerHTML=\`<div class=\"ss-title\"><strong>Naver SmartStore 自助取價 Beta</strong><span class=\"ss-badge\">本人驗證後讀價</span></div><p class=\"ss-help\">Naver 若要求安全驗證，請在 Naver 官方商品頁由您本人完成。完成後回來，桌機可分享已驗證的 Naver 分頁；手機可上傳商品頁截圖。系統會在您的瀏覽器中辨識韓幣價格，再套用本站即時匯率與代購公式。</p><div class=\"ss-translate\"><b>韓文原文＋中文協助</b><br>자동입력 방지 / 보안 확인 → 自動輸入防止／安全驗證，請依 Naver 原頁提示本人完成。<br>로그인이 필요합니다 → 需要登入；帳號密碼請只在 Naver 官方頁面輸入，本站不會要求或保存。<br>현재 서비스 접속이 불가합니다 → 目前服務暫時限制連線，請稍後重新整理 Naver 商品頁。</div><div class=\"ss-actions\"><a class=\"primary\" id=\"ssOpen\" target=\"_blank\" rel=\"noopener\">① 開啟 Naver 商品頁並完成驗證</a><button type=\"button\" id=\"ssShare\">② 分享已驗證的 Naver 分頁讀價</button><button type=\"button\" id=\"ssUploadBtn\">手機／備援：上傳商品頁截圖</button><input class=\"ss-upload\" id=\"ssUpload\" type=\"file\" accept=\"image/*\"></div><div class=\"ss-status\" id=\"ssStatus\">尚未讀取畫面。</div><div class=\"ss-candidates\" id=\"ssCandidates\"></div><p class=\"ss-private\">隱私：OCR 在此瀏覽器內執行；不要分享或上傳含 Naver 密碼、付款資料的畫面。本站只需要商品價格區域。</p>\`;host.insertAdjacentElement('afterend',box);\nconst open=$('#ssOpen'),share=$('#ssShare'),upBtn=$('#ssUploadBtn'),upload=$('#ssUpload'),status=$('#ssStatus'),cands=$('#ssCandidates');\nfunction sync(){const v=url.value.trim(),ok=isSmart(v);box.classList.toggle('show',ok);if(ok)open.href=v;}url.addEventListener('input',sync);url.addEventListener('change',sync);sync();\nasync function loadOCR(){if(window.Tesseract)return window.Tesseract;status.innerHTML='<span class=\"ss-spinner\"></span>正在載入本機文字辨識工具…';await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js';s.onload=resolve;s.onerror=reject;document.head.appendChild(s)});return window.Tesseract;}\nfunction priceCandidates(text){const raw=(text.match(/(?:₩\\s*)?\\d{1,3}(?:[,.]\\d{3})+|(?:₩\\s*)?\\d{4,8}/g)||[]);const map=new Map();for(const token of raw){const n=Number(token.replace(/[^0-9]/g,''));if(!Number.isFinite(n)||n<1000||n>10000000)continue;let score=0;if(/[,.]/.test(token))score+=4;if(token.includes('₩'))score+=4;const count=(map.get(n)?.count||0)+1;score+=Math.min(count,3);map.set(n,{n,count,score:(map.get(n)?.score||0)+score});}return [...map.values()].sort((a,b)=>b.score-a.score||b.count-a.count||b.n-a.n).slice(0,8).map(x=>x.n)}\nasync function recognize(source){cands.innerHTML='';const T=await loadOCR();status.innerHTML='<span class=\"ss-spinner\"></span>正在辨識商品頁上的韓幣金額，第一次約需數秒…';const out=await T.recognize(source,'eng',{logger:m=>{if(m.status==='recognizing text')status.innerHTML='<span class=\"ss-spinner\"></span>辨識中 '+Math.round((m.progress||0)*100)+'%';}});const list=priceCandidates(out.data.text||'');show(list,out.data.text||'');}\nfunction show(list,text){if(!list.length){status.textContent='沒有可靠辨識到價格。請把 Naver 商品售價區域放大後再分享，或截圖只保留商品價格附近。';return;}status.textContent='辨識到以下韓幣金額，請點選「商品售價」；選擇後會直接帶入本站既有報價公式。';for(const n of list){const b=document.createElement('button');b.type='button';b.className='ss-price';b.innerHTML='₩ '+n.toLocaleString('ko-KR')+'　<span style=\"font-size:11px;color:#617565\">點選此商品售價</span>';b.onclick=()=>apply(n);cands.appendChild(b);}window.__smartstoreLastOCR=text;}\nfunction apply(n){const d=$('.manual-details');if(d)d.open=true;const p=$('#p'),s=$('#s'),pf=$('#pf');if(!p){status.textContent='找不到本站手動報價欄位，請重新整理後再試。';return;}p.value=String(n);p.dispatchEvent(new Event('input',{bubbles:true}));if(s){s.value='';s.dispatchEvent(new Event('input',{bubbles:true}))}if(pf){pf.value='';pf.dispatchEvent(new Event('input',{bubbles:true}))}const btn=$('#manual button');if(btn)btn.click();status.textContent='已把 ₩'+n.toLocaleString('ko-KR')+' 帶入商品售價，並使用本站目前即時匯率重新試算。';setTimeout(()=>$('#result')?.scrollIntoView({behavior:'smooth',block:'start'}),100);}\nasync function captureTab(){if(!navigator.mediaDevices?.getDisplayMedia){status.textContent='這個瀏覽器不支援分頁分享取價，請改用「上傳商品頁截圖」。';return;}let stream;try{stream=await navigator.mediaDevices.getDisplayMedia({video:{displaySurface:'browser'},audio:false});const v=document.createElement('video');v.srcObject=stream;v.muted=true;await v.play();await new Promise(r=>setTimeout(r,500));const c=document.createElement('canvas');c.width=v.videoWidth;c.height=v.videoHeight;const x=c.getContext('2d');x.drawImage(v,0,0);stream.getTracks().forEach(t=>t.stop());await recognize(c);}catch(e){if(stream)stream.getTracks().forEach(t=>t.stop());status.textContent=e?.name==='NotAllowedError'?'你取消了分頁分享。完成 Naver 驗證後，可再按一次並選擇 Naver 商品分頁。':'讀取分頁失敗：'+(e?.message||e);}}\nshare.addEventListener('click',captureTab);upBtn.addEventListener('click',()=>upload.click());upload.addEventListener('change',async()=>{const f=upload.files?.[0];if(!f)return;try{await recognize(f)}catch(e){status.textContent='截圖辨識失敗：'+(e?.message||e)}});\nwindow.__fatfatpigSmartstoreBeta={isSmart,priceCandidates,apply,show};\n})();\n`;
files.set('smartstore-beta.js',Buffer.from(beta));
let html=files.get('index.html').toString('utf8');assert.ok(html.includes('2026-09-11.15')||html.includes('20260911-15'));html=html.replaceAll('2026-09-11.15',VERSION).replaceAll('20260911-15','20260911-16');assert.equal((html.match(/smartstore-beta\.js/g)||[]).length,0);html=html.replace('</body>','<script src="/smartstore-beta.js?v=20260911-16" defer></script>\n</body>');files.set('index.html',Buffer.from(html));
let platform=files.get('lib/platforms.mjs').toString();assert.ok(platform.includes('2026-09-11.15'));files.set('lib/platforms.mjs',Buffer.from(platform.replace('2026-09-11.15',VERSION)));const pkg=JSON.parse(files.get('package.json'));pkg.version='1.0.16';files.set('package.json',Buffer.from(JSON.stringify(pkg,null,2)+'\n'));
const changed=new Set(['smartstore-beta.js','index.html','lib/platforms.mjs','package.json']);for(const[f,b]of files)if(!changed.has(f))assert.equal(sha(b),base.hashes[f],f+' must remain unchanged');
const write=(f,b)=>{fs.mkdirSync(path.dirname(f),{recursive:true});fs.writeFileSync(f,b)};const root='.vercel/output';fs.rmSync(root,{recursive:true,force:true});for(const[file,b]of files)if(!file.startsWith('api/')&&file!=='package.json')write(root+'/static/'+file,b);for(const endpoint of ['health','quote','rate']){const dir=root+'/functions/api/'+endpoint+'.func';for(const[file,b]of files)if(file.startsWith('lib/'))write(dir+'/'+file,b);write(dir+'/api/'+endpoint+'.js',files.get('api/'+endpoint+'.js'));write(dir+'/package.json',JSON.stringify({type:'module'}));write(dir+'/.vc-config.json',JSON.stringify({runtime:'nodejs22.x',handler:'api/'+endpoint+'.js',launcherType:'Nodejs',shouldAddHelpers:true,maxDuration:endpoint==='quote'?60:15}));}write(root+'/config.json',JSON.stringify({version:3,routes:[{src:'/(.*)',headers:{'X-Content-Type-Options':'nosniff','Referrer-Policy':'no-referrer'},continue:true},{handle:'filesystem'}]}));
const release={version:VERSION,changeType:'smartstore-self-service-verified-tab-ocr-beta',existingCalculationsUnchanged:true,privacy:'ocr-runs-in-customer-browser; no captcha solving; user completes Naver verification',hashes:Object.fromEntries([...files].map(([f,b])=>[f,sha(b)])),files:[...files].map(([file,b])=>({file,encoding:'base64',data:b.toString('base64')}))};fs.writeFileSync('prepared-v16.json',JSON.stringify(release));console.log('V16_PREPARED',JSON.stringify({version:VERSION,changed:[...changed],fileCount:files.size,sha256:sha(JSON.stringify(release))}));